import 'package:connects_you/widgets/screens/main.dart';
import 'package:flutter/material.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const Root());
}
// create currentUser box (for current user)
// create users box (for chats)
// create rooms box (for rooms)
// create sharedKeys box (for shared keys)